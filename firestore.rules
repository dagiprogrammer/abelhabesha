rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Products collection:
    // Public catalog read: anyone can view products
    // Admin write: only authenticated admins can create, update, or delete products
    match /products/{productId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
